# Deployment del microservicio de autenticación.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-auth
  namespace: microservicios
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-auth
  template:
    metadata:
      labels:
        app: ms-auth
    spec:
      containers:
        - name: ms-auth
          image: ms-auth:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          env:
            - name: PORT
              value: "8083"
            # Conexión a PostgreSQL (DNS interno de Kubernetes).
            - name: DB_HOST
              value: auth-postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: auth-secrets
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: auth-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auth-secrets
                  key: DB_PASSWORD
            # Clave para firmar/verificar los JWT.
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: auth-secrets
                  key: JWT_SECRET
            - name: JWT_EXPIRES
              value: "24h"
---
# Service ClusterIP expuesto al Ingress en el puerto 8083.
apiVersion: v1
kind: Service
metadata:
  name: ms-auth
  namespace: microservicios
spec:
  selector:
    app: ms-auth
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP